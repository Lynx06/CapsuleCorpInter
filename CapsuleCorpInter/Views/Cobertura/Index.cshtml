@model IEnumerable<CapsuleCorpInter.Views.Cobertura>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Adicionar nova Cobertura", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CoberturaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CoberturaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CoberturaId })
        </td>
    </tr>
}

</table>
